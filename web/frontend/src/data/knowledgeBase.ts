// Issue Knowledge Base - WHAT/WHY/HOW framework for junior engineers
// Each issue type maps to a clear explanation with actionable solutions

export interface IssueKnowledge {
  code: string;
  icon: string;
  category: 'security' | 'performance' | 'stability' | 'infrastructure' | 'sdwan' | 'application';
  what: string;
  why: string;
  how: string[];
  eli5: string;
  severity: 'critical' | 'warning' | 'info';
  tacTip?: string;
  fixRate?: string;
  wiresharkFilter?: string;
  references?: string[];
  commands?: string[];
}

export const issueKnowledgeBase: Record<string, IssueKnowledge> = {
  // ─── SECURITY: DDoS ────────────────────────────────────────
  ddos_syn_flood: {
    code: 'DDOS_SYN_FLOOD',
    icon: '🔴',
    category: 'security',
    what: 'A SYN flood attack was detected. One source is sending massive numbers of TCP SYN packets to overwhelm the target.',
    why: 'SYN floods exhaust the target server\'s connection table, preventing legitimate users from connecting. This causes application timeouts and service outages.',
    how: [
      'Check if the source IP is internal or external — if external, block it at the perimeter firewall',
      'Enable SYN cookies on the target server: sysctl -w net.ipv4.tcp_syncookies=1',
      'Configure rate limiting on the SD-WAN edge using your vendor\'s QoS/policing feature',
      'Create a security policy on your SD-WAN platform to drop traffic from the source IP',
      'Verify the attack has stopped by monitoring connection rates',
    ],
    eli5: 'Imagine someone keeps knocking on your door and running away. You keep going to answer it, so you can\'t do anything else. That\'s what a SYN flood does to a server.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for packet drop statistics and session table counters from the edge device',
    fixRate: 'Blocking the source IP fixes 90% of cases',
    wiresharkFilter: 'tcp.flags.syn==1 && tcp.flags.ack==0',
    commands: [
      'Check firewall/ACL hit counters on the SD-WAN edge',
      'Review packet drop statistics on the edge device',
      'Verify security policy is applied from the SD-WAN controller',
    ],
  },

  ddos_udp_flood: {
    code: 'DDOS_UDP_FLOOD',
    icon: '🔴',
    category: 'security',
    what: 'A UDP flood attack was detected. Large volumes of UDP packets are being sent to random ports on the target.',
    why: 'UDP floods consume bandwidth and processing power. The target wastes resources sending ICMP "port unreachable" responses, degrading performance for all users.',
    how: [
      'Identify the source IP and check if it\'s a legitimate device or compromised host',
      'Apply a firewall rule or ACL to block UDP traffic from the source IP on the SD-WAN edge',
      'Enable uRPF (unicast Reverse Path Forwarding) on the ingress interface to drop spoofed packets',
      'Configure QoS to rate-limit UDP traffic on the WAN interface',
      'Monitor with: show interface <intf> | include drops',
    ],
    eli5: 'Imagine someone flooding your mailbox with junk mail so fast that real letters can\'t get through. That\'s a UDP flood.',
    severity: 'critical',
    tacTip: 'Check the routing table for the source — if it arrives via a tunnel, the attack is coming through the SD-WAN overlay',
    fixRate: 'ACL + rate limiting fixes 85% of cases',
    wiresharkFilter: 'udp && ip.src == <source_ip>',
  },

  ddos_icmp_flood: {
    code: 'DDOS_ICMP_FLOOD',
    icon: '🟡',
    category: 'security',
    what: 'An ICMP flood (ping flood) was detected. A host is sending excessive ICMP echo requests.',
    why: 'While less dangerous than SYN/UDP floods, ICMP floods still consume bandwidth and can mask other attacks. They may indicate a compromised internal host.',
    how: [
      'Rate-limit ICMP on the interface: ip icmp rate-limit unreachable 500',
      'Check if the source is an internal device that may be compromised',
      'Apply a firewall rule or ACL to rate-limit ICMP on the SD-WAN edge or perimeter firewall',
      'On SD-WAN, create a policy to deprioritize ICMP traffic',
      'Verify with: ping <target> — if response times normalize, fix is working',
    ],
    eli5: 'Someone is ping-ing a server thousands of times per second, like pressing a doorbell non-stop.',
    severity: 'warning',
    wiresharkFilter: 'icmp.type == 8',
  },

  // ─── SECURITY: Port Scanning ───────────────────────────────
  port_scan: {
    code: 'PORT_SCAN',
    icon: '🔍',
    category: 'security',
    what: 'Port scanning activity was detected. A host is probing multiple ports on one or more targets to discover open services.',
    why: 'Port scanning is often the first step of an attack — the attacker maps out which services are running to find vulnerabilities. This could indicate reconnaissance by a threat actor or a misconfigured monitoring tool.',
    how: [
      'Identify the source IP — is it an authorized scanner (Nessus, Qualys) or unknown?',
      'If unauthorized, block immediately using your SD-WAN platform\'s firewall/ACL feature',
      'Check internal hosts for malware: run an endpoint scan on the source device',
      'Enable IPS/IDS signatures for port scanning on the firewall',
      'Review SD-WAN security policy to ensure proper segmentation',
    ],
    eli5: 'Someone is trying every door and window in your building to see which ones are unlocked.',
    severity: 'warning',
    tacTip: 'If the source is internal, this is likely a compromised host — escalate to the security team immediately',
    fixRate: 'Identify source first — 70% are misconfigured scanners, 30% are actual threats',
    wiresharkFilter: 'tcp.flags.syn==1 && tcp.flags.ack==0',
  },

  // ─── SECURITY: DNS ─────────────────────────────────────────
  dns_anomaly: {
    code: 'DNS_ANOMALY',
    icon: '🌐',
    category: 'security',
    what: 'Suspicious DNS activity was detected. This could include unusual query patterns, potential DNS tunneling, or queries to known malicious domains.',
    why: 'DNS anomalies can indicate data exfiltration (DNS tunneling), malware communication (C2 beaconing), or DNS hijacking. Even legitimate-looking DNS issues can cause application failures.',
    how: [
      'Check if the DNS server IP is authorized — compare against known DNS servers in your network',
      'Look for unusually long domain names (>50 chars) which indicate DNS tunneling',
      'Verify DNS responses match expected IP ranges for the domain',
      'Check DNS query frequency — >100 queries/min to the same domain is suspicious',
      'Configure DNS security on SD-WAN: Umbrella/Zscaler integration or local DNS policy',
    ],
    eli5: 'DNS is like a phone book for the internet. This alert means someone might be using fake phone book entries or hiding secret messages in phone number lookups.',
    severity: 'warning',
    wiresharkFilter: 'dns',
    commands: [
      'nslookup <domain> <dns_server>',
      'dig @<dns_server> <domain> ANY',
    ],
  },

  // ─── SECURITY: TLS ─────────────────────────────────────────
  tls_weakness: {
    code: 'TLS_WEAKNESS',
    icon: '🔓',
    category: 'security',
    what: 'A TLS security weakness was detected. This could be an outdated TLS version (1.0/1.1), weak cipher suite, or missing Perfect Forward Secrecy.',
    why: 'Weak TLS allows attackers to decrypt traffic. TLS 1.0/1.1 have known vulnerabilities (BEAST, POODLE). Weak ciphers can be broken with modern computing power. This is a compliance violation for PCI-DSS, HIPAA, and most security frameworks.',
    how: [
      'Identify which server is using weak TLS — check the server_ip and port in the finding',
      'If it\'s your server: update TLS configuration to require TLS 1.2+ minimum',
      'Disable weak ciphers: SSLCipherSuite HIGH:!aNULL:!MD5:!3DES:!RC4',
      'If it\'s an external service, contact the vendor to request TLS upgrade',
      'On SD-WAN, use application-aware routing to route this traffic through an inspection point',
    ],
    eli5: 'This is like using a cheap lock on your front door that anyone with a YouTube tutorial could pick. You need a better lock.',
    severity: 'warning',
    tacTip: 'Vendor support will ask for the TLS version and cipher suite in use on the SD-WAN control plane connections',
    fixRate: 'Server-side TLS update fixes 95% of cases',
    wiresharkFilter: 'tls.handshake.version',
  },

  // ─── SECURITY: ARP ─────────────────────────────────────────
  arp_conflict: {
    code: 'ARP_CONFLICT',
    icon: '⚠️',
    category: 'security',
    what: 'Multiple MAC addresses are claiming the same IP address on the network. This is an ARP conflict.',
    why: 'ARP conflicts cause intermittent connectivity — traffic randomly goes to the wrong device. This could be a misconfiguration (duplicate IPs) or an ARP spoofing attack (man-in-the-middle).',
    how: [
      'Check both MAC addresses — look up the switch port for each MAC in your switch\'s MAC address table',
      'Determine if one device is rogue: check OUI (first 3 octets of MAC) against vendor lookup',
      'If duplicate IP: reconfigure one device with a unique IP address',
      'If ARP spoofing: enable Dynamic ARP Inspection (DAI) on the switch',
      'Verify fix: clear ARP cache on affected devices and monitor for recurrence',
    ],
    eli5: 'Two people in an office both claim to sit at desk #5. Mail meant for desk #5 randomly goes to either person.',
    severity: 'warning',
    wiresharkFilter: 'arp',
    commands: [
      'Check ARP table for the conflicting IP on the SD-WAN edge',
      'Check MAC address table on the LAN switch to locate the device port',
      'Check DHCP binding table to verify which server assigned the IP',
    ],
  },

  // ─── PERFORMANCE: TCP ──────────────────────────────────────
  tcp_retransmission: {
    code: 'TCP_RETRANSMISSION',
    icon: '🔄',
    category: 'performance',
    what: 'High TCP retransmission rate detected. Packets are being lost and re-sent, indicating network path issues.',
    why: 'Retransmissions add latency (typically 200ms+ per retransmit), reduce throughput, and indicate packet loss on the network path. >1% retransmission rate degrades user experience; >5% causes application timeouts.',
    how: [
      'Identify the worst-affected flows — check source/destination IPs and ports',
      'Check the SD-WAN tunnel health using your vendor\'s tunnel statistics command or GUI',
      'Look for interface errors (drops, CRC errors) on the WAN interface',
      'Check QoS policy — ensure critical traffic is in the right queue',
      'If on MPLS + Internet dual-WAN, check if failover is causing the issue',
      'Consider enabling FEC (Forward Error Correction) on the SD-WAN tunnel',
    ],
    eli5: 'Imagine sending a letter but it keeps getting lost in the mail, so you have to keep sending copies. Each copy takes time, slowing everything down.',
    severity: 'warning',
    tacTip: 'Vendor support will ask for per-tunnel loss/latency/jitter statistics from both endpoints',
    fixRate: 'FEC or tunnel optimization fixes 75% of cases',
    wiresharkFilter: 'tcp.analysis.retransmission',
    commands: [
      'Check SD-WAN tunnel statistics on the edge device',
      'Check per-application route statistics for loss/latency/jitter',
      'Check interface counters for errors and drops on the WAN interface',
    ],
  },

  tcp_handshake_failure: {
    code: 'TCP_HANDSHAKE_FAILURE',
    icon: '❌',
    category: 'performance',
    what: 'TCP handshake failures detected. Connections to certain servers are failing to establish.',
    why: 'Failed handshakes mean applications cannot connect to their servers. Users see "Connection timed out" or "Connection refused" errors. This could indicate a down server, firewall blocking, or routing issue.',
    how: [
      'Check if the destination server is reachable: ping <dest_ip>',
      'Verify no firewall is blocking the port: telnet <dest_ip> <port>',
      'Check SD-WAN overlay routing to verify the path to the destination subnet',
      'Look for asymmetric routing — SYN goes one path, SYN-ACK returns another',
      'Check if NAT is configured correctly on the SD-WAN edge',
    ],
    eli5: 'You\'re trying to call someone but they never pick up the phone. Either their phone is off, or something is blocking the call.',
    severity: 'warning',
    tacTip: 'Vendor support will ask for the SD-WAN service path or routing table output for the destination subnet',
    wiresharkFilter: 'tcp.flags.syn==1 && !tcp.flags.ack',
  },

  // ─── PERFORMANCE: Packet Loss ──────────────────────────────
  packet_loss: {
    code: 'PACKET_LOSS',
    icon: '📉',
    category: 'performance',
    what: 'Significant packet loss detected on the network. Packets are being dropped before reaching their destination.',
    why: 'Packet loss directly impacts application performance. For voice/video, even 1% loss causes noticeable quality degradation. For data applications, TCP retransmissions reduce throughput by 10-50x.',
    how: [
      'Identify where loss is occurring: is it on the LAN, WAN, or tunnel?',
      'Check interface counters for drops and errors on the WAN interface',
      'Check SD-WAN path health metrics (BFD/keepalive statistics)',
      'Look for QoS tail drops in the policy-map or traffic shaper statistics',
      'Check for congestion: look for output drops on the WAN interface',
      'Consider upgrading WAN bandwidth if consistently over 80% utilization',
    ],
    eli5: 'Packets are like trucks carrying data. Some trucks are falling off the road before they reach the destination. We need to find where and why.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for path health metrics, tunnel statistics, and interface drop counters from both endpoints',
    fixRate: 'QoS tuning fixes 60% of cases; bandwidth upgrade fixes 30%',
    wiresharkFilter: 'tcp.analysis.lost_segment',
  },

  // ─── PERFORMANCE: RTT/Latency ─────────────────────────────
  high_latency: {
    code: 'HIGH_LATENCY',
    icon: '🐌',
    category: 'performance',
    what: 'High round-trip time (RTT) detected on network flows. Packets are taking longer than expected to reach their destination and return.',
    why: 'High latency causes slow page loads, sluggish applications, poor voice quality (>150ms is noticeable), and video buffering. For SD-WAN, it may indicate suboptimal path selection.',
    how: [
      'Check the SD-WAN tunnel health using your vendor\'s tunnel statistics command or GUI',
      'Compare latency across available WAN transports',
      'Verify SLA policy is configured to prefer low-latency paths for sensitive traffic',
      'Check for geographic routing issues — is traffic taking a longer path than necessary?',
      'Consider enabling application-aware routing to dynamically select best path',
    ],
    eli5: 'Data packets are taking the scenic route instead of the highway. We need to find them a faster path.',
    severity: 'warning',
    wiresharkFilter: 'tcp.analysis.ack_rtt > 0.1',
  },

  // ─── STABILITY: VRRP ──────────────────────────────────────
  vrrp_flapping: {
    code: 'VRRP_FLAPPING',
    icon: '🔃',
    category: 'stability',
    what: 'VRRP (Virtual Router Redundancy Protocol) is flapping — the master router role is switching back and forth rapidly.',
    why: 'VRRP flapping causes brief network outages (200ms-2s) each time the master changes. Users experience intermittent connectivity drops, failed transactions, and voice call drops.',
    how: [
      'Check both VRRP routers for interface errors on the shared LAN segment',
      'Verify VRRP timers match on both routers (check VRRP status on each)',
      'Check for switch port issues between the routers (CRC errors, flapping)',
      'Ensure preempt delay is configured to prevent rapid failbacks',
      'Check CPU usage on both routers — high CPU can delay VRRP hello packets',
    ],
    eli5: 'Two routers keep fighting over who\'s in charge, like two people grabbing a steering wheel. Every time they switch, the car swerves.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for VRRP/FHRP detail status and system logs from both routers',
    fixRate: 'Timer tuning and preempt delay fix 80% of cases',
    wiresharkFilter: 'vrrp',
    commands: [
      'Check VRRP/FHRP status summary on both routers',
      'Check VRRP/FHRP detail output including timers and priority on both routers',
      'Check system logs for gateway redundancy state change events',
    ],
  },

  // ─── STABILITY: HSRP ──────────────────────────────────────
  hsrp_instability: {
    code: 'HSRP_INSTABILITY',
    icon: '🔃',
    category: 'stability',
    what: 'HSRP (Hot Standby Router Protocol) state changes detected. The active/standby roles may be shifting.',
    why: 'HSRP state changes cause default gateway failover, resulting in brief connectivity loss for all devices using that gateway. Frequent changes indicate an underlying infrastructure problem.',
    how: [
      'Check HSRP status on both routers (active/standby roles)',
      'Verify HSRP timers on both routers match',
      'Check the link between routers for errors',
      'Ensure HSRP priority and preempt settings are intentional',
      'Look for interface tracking issues that may trigger failover',
    ],
    eli5: 'The backup gateway router keeps taking over from the main one, causing brief internet outages each time.',
    severity: 'warning',
    wiresharkFilter: 'hsrp',
  },

  // ─── STABILITY: BFD Flapping ────────────────────────────────
  bfd_flapping: {
    code: 'BFD_FLAPPING',
    icon: '⚡',
    category: 'stability',
    what: 'BFD (Bidirectional Forwarding Detection) sessions are flapping — the link-health probes between routers are rapidly alternating between Up and Down states.',
    why: 'BFD is designed to detect link failures in milliseconds. When BFD flaps, every protocol relying on it (BGP, OSPF, IPsec, SD-WAN tunnels) tears down and reconverges. Users experience repeated 1-5 second outages, voice calls drop, and VPN tunnels bounce.',
    how: [
      'Check the physical WAN link for errors, CRC, input/output drops (show interface counters)',
      'Verify BFD Tx/Rx intervals and multiplier — aggressive timers on lossy links cause false flaps',
      'Check ISP for micro-outages or packet loss on the circuit',
      'Review SD-WAN path health / SLA metrics for the affected transport',
      'If on MPLS/Metro Ethernet, check provider CE-PE link for L2 errors',
      'Consider increasing BFD detect multiplier from 3 to 5 on lossy WAN links',
    ],
    eli5: 'The "heartbeat monitor" between two routers keeps saying the link is dead, then alive, then dead again — like a heart monitor beeping erratically. Every beep causes the network to panic and reroute.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for BFD neighbor detail, interface error counters, and ISP circuit ID for the affected link',
    fixRate: 'BFD timer tuning or ISP circuit repair fixes 85% of cases',
    wiresharkFilter: 'bfd',
    commands: [
      'Check BFD session detail for Tx/Rx intervals and detect multiplier',
      'Check interface error counters on both endpoints',
      'Check system logs for BFD and link-state events',
    ],
  },

  // ─── STABILITY: IKE Tunnel Rebuild ─────────────────────────
  ike_tunnel_rebuild: {
    code: 'IKE_TUNNEL_REBUILD',
    icon: '🔐',
    category: 'stability',
    what: 'IPsec VPN tunnels are repeatedly tearing down and rebuilding. Multiple IKE_SA_INIT (Phase 1) negotiations detected from the same peer in a short window.',
    why: 'Each tunnel rebuild causes 2-10 seconds of traffic loss while IKE renegotiates keys and IPsec SAs are re-established. Frequent rebuilds indicate the underlying WAN link is flapping or there is a configuration mismatch (DPD timers, lifetime, crypto mismatch).',
    how: [
      'Check IPsec tunnel status and uptime on both endpoints',
      'Review IKE/IPsec logs for Phase 1 or Phase 2 failure reasons',
      'Verify DPD (Dead Peer Detection) timers — aggressive DPD on a lossy link causes false teardowns',
      'Check if the underlay WAN link (BFD, ICMP) shows flapping',
      'Verify IKE lifetime and rekey settings match on both peers',
      'Check for NAT/firewall changes that may be breaking UDP 500/4500',
    ],
    eli5: 'The encrypted tunnel between your offices keeps collapsing and being rebuilt from scratch — like a bridge that keeps falling and being reconstructed every few seconds.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for IKEv2 SA detail, IPsec SA detail, and debug crypto output from both peers',
    fixRate: 'Fixing the underlying WAN link or adjusting DPD timers resolves 75% of cases',
    wiresharkFilter: 'isakmp.exchangetype == 34 || isakmp.exchangetype == 2',
    commands: [
      'Check IKEv2 SA detail for tunnel uptime and negotiation state',
      'Check IPsec SA detail for packet counters and rekey events',
      'Check system logs for IKE/IPsec tunnel up/down events',
    ],
  },

  // ─── STABILITY: STP TCN Storm ──────────────────────────────
  stp_tcn_storm: {
    code: 'STP_TCN_STORM',
    icon: '🌳',
    category: 'stability',
    what: 'A Spanning Tree Protocol Topology Change Notification (TCN) storm has been detected — an excessive number of TCN BPDUs indicate a switch port is rapidly flapping.',
    why: 'Each TCN causes all switches in the STP domain to flush their MAC address tables and re-learn, flooding traffic to all ports. This causes broadcast storms, increased CPU on switches, and intermittent connectivity for all LAN users.',
    how: [
      'Identify the originating switch and port from STP detail output',
      'Check the port for physical issues: cable fault, bad SFP, speed/duplex mismatch',
      'Enable PortFast + BPDU Guard on access ports to prevent end-devices from sending BPDUs',
      'Consider enabling storm-control on access ports',
      'Check for loop detection — a physical loop causes continuous TCNs',
      'Review spanning-tree topology change counters per-VLAN',
    ],
    eli5: 'A network switch port keeps connecting and disconnecting, like someone plugging and unplugging a cable rapidly. Every time it happens, the entire LAN network has to re-learn where all devices are.',
    severity: 'critical',
    tacTip: 'Switch support will ask for spanning-tree detail per VLAN, interface status, and recent log entries showing LINK-3-UPDOWN events',
    fixRate: 'Identifying and fixing the flapping port resolves 90% of cases',
    wiresharkFilter: 'stp.type == 0x80 || (stp.type == 0x00 && stp.flags.tc == 1)',
    commands: [
      'Check spanning-tree detail for topology change counters per VLAN',
      'Check all interface status for err-disabled or flapping ports',
      'Check system logs for link up/down events in the last hour',
    ],
  },

  // ─── STABILITY: Interface Flapping (generic) ──────────────
  interface_flapping: {
    code: 'INTERFACE_FLAPPING',
    icon: '⚡',
    category: 'stability',
    what: 'Interface instability detected — a network interface is rapidly changing state, causing dependent protocols and services to fail repeatedly.',
    why: 'Interface flapping is a root cause for many network issues: BGP/OSPF reconvergence, IPsec tunnel rebuilds, HSRP/VRRP failovers, and STP recalculations. Each flap creates a cascade of failures across the network.',
    how: [
      'Identify the specific flapping interface from logs and protocol state changes',
      'Check physical layer: cable, SFP, patch panel, port on switch/router',
      'Check for speed/duplex mismatch or auto-negotiation failures',
      'Review ISP circuit health if it is a WAN link',
      'Check for environmental issues: heat, power, EMI near cables',
      'Consider enabling dampening on the interface to rate-limit state changes',
    ],
    eli5: 'A network connection keeps going on and off rapidly, like a light switch being flicked. Every time it happens, the whole network has to figure out a new path, causing everyone to lose connection briefly.',
    severity: 'critical',
    tacTip: 'Support will ask for interface error counters, optical power levels (if fiber), and log entries from the last hour',
    fixRate: 'Physical layer fix (cable/SFP replacement) resolves 70% of flapping issues',
    wiresharkFilter: 'bfd || hsrp || vrrp || isakmp || stp',
  },

  // ─── SD-WAN: Tunnel Issues ─────────────────────────────────
  tunnel_flapping: {
    code: 'TUNNEL_FLAPPING',
    icon: '🔌',
    category: 'sdwan',
    what: 'SD-WAN tunnel instability detected. Tunnels are going up and down, causing traffic rerouting.',
    why: 'Tunnel flapping forces traffic to reroute, causing packet loss during transitions. Applications experience intermittent failures, and real-time services (voice/video) are severely impacted.',
    how: [
      'Check SD-WAN tunnel status using your vendor\'s tunnel statistics command or GUI',
      'Verify path health keepalives (BFD or vendor-specific health checks)',
      'Check underlay WAN link health for errors, drops, and flapping',
      'Look for MTU issues causing fragmentation on the tunnel path',
      'Check NAT/firewall for blocking of SD-WAN control and data plane ports',
      'Review controller connectivity from the edge device',
    ],
    eli5: 'The secure highway between your offices keeps closing and reopening. Every time it closes, traffic has to find another route.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for tunnel statistics and path health metrics from both endpoints',
    fixRate: 'BFD timer tuning or underlay fix resolves 70% of cases',
    commands: [
      'Check SD-WAN tunnel statistics on the edge device',
      'Check path health / BFD session status on the edge device',
      'Check SD-WAN controller connectivity status',
    ],
  },

  // ─── SD-WAN: Vendor Detection ──────────────────────────────
  sdwan_vendor_detected: {
    code: 'SDWAN_VENDOR_DETECTED',
    icon: '🏢',
    category: 'sdwan',
    what: 'SD-WAN vendor-specific traffic was identified in the capture.',
    why: 'Knowing the SD-WAN vendor helps target troubleshooting. Each vendor has specific control/data plane protocols, management interfaces, and common failure modes.',
    how: [
      'Verify the detected vendor matches your deployment',
      'Check vendor-specific control plane health',
      'Review vendor-specific logs and diagnostics',
      'Use vendor documentation for targeted troubleshooting',
    ],
    eli5: 'We identified which brand of SD-WAN you\'re using, so we can give you more specific help.',
    severity: 'info',
  },

  // ─── INFRASTRUCTURE: VoIP ─────────────────────────────────
  voip_quality: {
    code: 'VOIP_QUALITY',
    icon: '📞',
    category: 'application',
    what: 'Voice over IP quality issues detected. Jitter, latency, or packet loss on RTP streams exceeds acceptable thresholds.',
    why: 'VoIP is extremely sensitive to network quality. Jitter >30ms causes choppy audio, latency >150ms causes conversation overlap, and >1% packet loss causes missing words.',
    how: [
      'Check QoS marking: voice should be DSCP EF (46)',
      'Verify QoS policy is applied on all interfaces including SD-WAN tunnels',
      'Check for bandwidth congestion on WAN links',
      'Enable voice-specific SLA on your SD-WAN platform (latency <150ms, loss <1%, jitter <30ms)',
      'Consider dedicated WAN bandwidth for voice traffic',
    ],
    eli5: 'Phone calls are breaking up because the network is too busy or too slow. We need to give voice traffic a VIP lane.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for QoS policy configuration and traffic shaper statistics to verify voice prioritization',
    fixRate: 'QoS configuration fixes 85% of VoIP quality issues',
    wiresharkFilter: 'rtp',
  },

  // ─── INFRASTRUCTURE: HTTP Errors ──────────────────────────
  http_errors: {
    code: 'HTTP_ERRORS',
    icon: '🌐',
    category: 'application',
    what: 'HTTP error responses (4xx/5xx) detected in the traffic. Applications are returning errors to clients.',
    why: 'HTTP errors indicate application problems. 4xx errors mean client-side issues (authentication, missing resources). 5xx errors mean server-side failures. Patterns of errors indicate systemic problems.',
    how: [
      'Group errors by status code to identify the pattern',
      '401/403: Check authentication and authorization — credentials may have expired',
      '404: Verify the URL/path is correct — application may have been updated',
      '500/502/503: Check server health and load balancer configuration',
      'If errors correlate with SD-WAN path changes, check routing consistency',
    ],
    eli5: 'Websites and apps are showing error pages to users. We need to figure out if the problem is the app, the server, or the network.',
    severity: 'warning',
    wiresharkFilter: 'http.response.code >= 400',
  },

  // ─── INFRASTRUCTURE: STP ──────────────────────────────────
  stp_topology: {
    code: 'STP_TOPOLOGY',
    icon: '🌳',
    category: 'infrastructure',
    what: 'Spanning Tree Protocol topology information detected. STP is managing loop prevention on the Layer 2 network.',
    why: 'STP topology changes can cause brief network outages (30-50 seconds with classic STP). Understanding STP topology helps identify potential loop risks and optimize network paths.',
    how: [
      'Verify the root bridge is the intended switch: show spanning-tree root',
      'Check for topology change notifications: show spanning-tree detail | include change',
      'Ensure PortFast is configured on access ports: spanning-tree portfast',
      'Consider migrating to Rapid STP (RSTP) for faster convergence',
      'Check for BPDU guard violations: show spanning-tree inconsistentports',
    ],
    eli5: 'STP is like a traffic cop that prevents data from going in circles. If the traffic cop changes, there\'s a brief pause while everyone figures out the new rules.',
    severity: 'info',
    wiresharkFilter: 'stp',
  },

  // ─── INFRASTRUCTURE: CDP/LLDP ─────────────────────────────
  network_discovery: {
    code: 'NETWORK_DISCOVERY',
    icon: '🗺️',
    category: 'infrastructure',
    what: 'Network discovery protocol (CDP/LLDP) information found. These protocols share device identity and capability information between neighbors.',
    why: 'CDP/LLDP data reveals the physical network topology — which devices are connected to which ports. This is invaluable for troubleshooting and documentation.',
    how: [
      'Use this information to build/verify your network topology diagram',
      'Check that software versions are current and consistent',
      'Verify device capabilities match your design (router, switch, phone, etc.)',
      'Consider disabling CDP/LLDP on external-facing interfaces for security',
    ],
    eli5: 'Network devices are introducing themselves to their neighbors, like people at a party saying "Hi, I\'m a router named R1 and I\'m running version 17.3".',
    severity: 'info',
  },

  // ─── GENERIC FALLBACKS ─────────────────────────────────────
  suspicious_traffic: {
    code: 'SUSPICIOUS_TRAFFIC',
    icon: '🔎',
    category: 'security',
    what: 'Unusual or potentially malicious network traffic patterns were detected.',
    why: 'Suspicious traffic may indicate malware, data exfiltration, unauthorized access, or misconfigured applications. Early detection prevents potential security breaches.',
    how: [
      'Review the source and destination IPs — are they known and authorized?',
      'Check the protocol and ports — are they expected for your environment?',
      'Compare traffic volume against baseline — is this a sudden spike?',
      'Run endpoint security scan on involved hosts',
      'If confirmed malicious, isolate the source and escalate to security team',
    ],
    eli5: 'Something unusual is happening on the network that we should investigate further.',
    severity: 'warning',
  },

  tls_certificate_issue: {
    code: 'TLS_CERT_ISSUE',
    icon: '📜',
    category: 'security',
    what: 'TLS certificate issues detected — expired, self-signed, or misconfigured certificates.',
    why: 'Certificate issues cause browser warnings, application connection failures, and security vulnerabilities. Self-signed certificates on production systems indicate a security risk.',
    how: [
      'Check certificate expiration date — renew if expired',
      'If self-signed, replace with a certificate from a trusted CA',
      'Verify the certificate chain is complete (no missing intermediate CAs)',
      'Check that the server name matches the certificate\'s CN or SAN',
      'For SD-WAN, verify the Comodo/Symantec root CA is trusted',
    ],
    eli5: 'A website\'s ID card (certificate) is either expired, fake, or has the wrong name. Users will see scary warning messages.',
    severity: 'warning',
    wiresharkFilter: 'tls.handshake.certificate',
  },

  // ─── INFRASTRUCTURE: DHCP ──────────────────────────────────
  dhcp_rogue_server: {
    code: 'DHCP_ROGUE_SERVER',
    icon: '🏴',
    category: 'infrastructure',
    what: 'Multiple DHCP servers detected on the network. One may be unauthorized (rogue).',
    why: 'A rogue DHCP server can hand out wrong IP addresses, wrong gateways, or wrong DNS servers — causing clients to lose connectivity or route traffic through an attacker\'s machine (man-in-the-middle).',
    how: [
      'Identify all DHCP servers by their MAC and IP addresses in the findings',
      'Verify which server is authorized by checking your DHCP server configuration',
      'Enable DHCP snooping on your switches to block unauthorized DHCP responses',
      'Configure DHCP snooping on the LAN switch: enable globally, per-VLAN, and trust the uplink port',
      'Physically locate and disconnect the rogue device if it\'s malicious',
    ],
    eli5: 'Imagine two people handing out house addresses in a neighborhood — one is the real postman, the other is a stranger giving wrong addresses. Clients end up at the wrong place.',
    severity: 'critical',
    tacTip: 'Vendor support will ask for DHCP snooping binding table and statistics from the LAN switch, plus DHCP server logs',
    fixRate: 'Enabling DHCP snooping fixes 95% of rogue server issues',
    wiresharkFilter: 'bootp.option.dhcp == 2',
    commands: [
      'Check DHCP snooping status and trust port configuration on the LAN switch',
      'Check DHCP snooping binding table to identify authorized vs rogue servers',
      'Check DHCP server statistics for offer/ack/nak counts',
    ],
  },

  dhcp_starvation: {
    code: 'DHCP_STARVATION',
    icon: '🏴',
    category: 'security',
    what: 'A DHCP starvation attack was detected — one device is flooding DHCP DISCOVER requests to exhaust the IP address pool.',
    why: 'When the DHCP pool is exhausted, new legitimate devices cannot get an IP address and cannot join the network. This is a denial-of-service attack against the DHCP infrastructure.',
    how: [
      'Identify the attacking MAC address from the findings',
      'Enable DHCP snooping rate limiting on switch ports: ip dhcp snooping limit rate 10',
      'Enable port security to limit MAC addresses per port',
      'Block the offending MAC address at the switch port level',
      'Consider 802.1X authentication to prevent unauthorized devices',
    ],
    eli5: 'Someone is grabbing all the parking spots in a lot by putting fake cars in each one. Now real cars have nowhere to park.',
    severity: 'critical',
    wiresharkFilter: 'bootp.option.dhcp == 1',
    commands: [
      'Check DHCP snooping statistics on the LAN switch',
      'Check DHCP pool utilization on the DHCP server',
      'Check MAC address table on the LAN switch to locate the attacker',
    ],
  },

  dhcp_nak_storm: {
    code: 'DHCP_NAK_STORM',
    icon: '⚠️',
    category: 'infrastructure',
    what: 'The DHCP server is sending an unusually high number of NAK (negative acknowledgment) responses.',
    why: 'DHCP NAK storms indicate the server is rejecting client requests — often because clients are requesting IPs from a wrong subnet, the pool is misconfigured, or there\'s a scope mismatch after a network change.',
    how: [
      'Check the DHCP server logs for the reason behind the NAKs',
      'Verify the DHCP scope matches the subnet configuration',
      'Check if clients have stale leases from a previous subnet',
      'Force clients to release and renew: ipconfig /release then ipconfig /renew',
      'Verify DHCP relay agent configuration if clients are on a different VLAN',
    ],
    eli5: 'The DHCP server keeps saying "No!" to devices asking for an IP address. Something is wrong with the address pool or the requests.',
    severity: 'warning',
    wiresharkFilter: 'bootp.option.dhcp == 6',
    commands: [
      'Check DHCP server statistics for NAK counts',
      'Check DHCP conflict table for address conflicts',
      'Check DHCP binding table for current leases',
    ],
  },

  // ─── INFRASTRUCTURE: NTP ───────────────────────────────────
  ntp_amplification: {
    code: 'NTP_AMPLIFICATION',
    icon: '🔴',
    category: 'security',
    what: 'NTP amplification attack detected — large NTP responses are being used to flood a target.',
    why: 'NTP amplification is a DDoS technique where attackers send small requests to NTP servers with a spoofed source IP. The server sends a much larger response to the victim, amplifying the attack by up to 556x.',
    how: [
      'Check if your NTP servers have monlist enabled — disable it immediately',
      'Restrict NTP server access to authorized clients only using ACLs',
      'Implement BCP38/uRPF to prevent IP spoofing on your network edge',
      'Rate-limit NTP traffic at the firewall',
      'Upgrade NTP servers to version 4.2.7+ which disables monlist by default',
    ],
    eli5: 'An attacker sends a tiny letter to a time server saying "tell me everything" but puts someone else\'s return address. The server sends a huge reply to the victim.',
    severity: 'critical',
    wiresharkFilter: 'ntp && udp.length > 468',
    commands: [
      'Check NTP synchronization status on the edge device (ntpq -p on Linux, or vendor CLI)',
      'Check NTP peer associations and stratum levels on the NTP server',
      'Check firewall rules filtering inbound NTP (UDP/123) from external sources',
    ],
  },

  ntp_stratum_change: {
    code: 'NTP_STRATUM_CHANGE',
    icon: '⏰',
    category: 'infrastructure',
    what: 'NTP server stratum level is changing frequently, indicating time source instability.',
    why: 'Stratum changes mean the NTP server is losing and regaining its upstream time source. This causes time drift across the network, which breaks certificate validation, log correlation, Kerberos authentication, and SD-WAN tunnel establishment.',
    how: [
      'Check the NTP server\'s upstream time source — is it reachable?',
      'Verify NTP server configuration — check peer associations and stratum on the NTP server',
      'Add redundant NTP sources for resilience',
      'Check for network issues between the NTP server and its upstream source',
      'Monitor time offset with "ntpq -p" on Linux or the equivalent NTP status command on your platform',
    ],
    eli5: 'The network\'s clock keeps changing which master clock it trusts. When clocks disagree, things like logins and security certificates break.',
    severity: 'warning',
    wiresharkFilter: 'ntp',
    commands: [
      'Check NTP synchronization status on the affected device',
      'Check NTP peer associations with detail (stratum, offset, jitter)',
      'Check current system clock and compare against a known-good time source',
    ],
  },

  // ─── SECURITY: DNS TUNNELING ───────────────────────────────
  dns_tunneling: {
    code: 'DNS_TUNNELING',
    icon: '🕳️',
    category: 'security',
    what: 'DNS tunneling activity suspected — a host is encoding data inside DNS queries to exfiltrate information or establish a covert communication channel.',
    why: 'DNS tunneling bypasses firewalls and security controls because DNS traffic is almost always allowed. Attackers use it for data exfiltration, C2 communication, and bypassing captive portals. It\'s a strong indicator of compromise.',
    how: [
      'Investigate the source host immediately — run endpoint detection scan',
      'Check the suspicious domain in threat intelligence databases (VirusTotal, AbuseIPDB)',
      'Block the suspicious base domain at your DNS resolver or firewall',
      'Enable DNS security features: Cisco Umbrella, Zscaler DNS, or Pi-hole',
      'Implement DNS query length limits and TXT record restrictions',
      'Review DNS logs for the full scope of the tunneling activity',
    ],
    eli5: 'Someone is hiding secret messages inside normal-looking website address lookups. It\'s like writing a secret note on the back of a library card request.',
    severity: 'critical',
    tacTip: 'Provide the suspicious domain and source IP to your security team for threat hunting',
    fixRate: 'Blocking the domain stops the tunnel immediately, but investigate the host for malware',
    wiresharkFilter: 'dns.qry.name.len > 50',
    commands: [
      'nslookup -type=TXT <suspicious-domain>',
      'dig +short TXT <suspicious-domain>',
      'show ip dns view',
    ],
  },

  // ─── SECURITY: C2 BEACONING ────────────────────────────────
  c2_beaconing: {
    code: 'C2_BEACONING',
    icon: '📡',
    category: 'security',
    what: 'Command-and-Control (C2) beaconing pattern detected — a host is communicating with an external server at regular intervals with consistent payload sizes.',
    why: 'C2 beaconing is a hallmark of malware infection. The compromised host periodically "checks in" with the attacker\'s server for new commands. Regular intervals and consistent payload sizes distinguish it from normal traffic.',
    how: [
      'IMMEDIATELY isolate the source host from the network',
      'Do NOT power off the host — preserve forensic evidence',
      'Check the destination IP/domain in threat intelligence (VirusTotal, AbuseIPDB)',
      'Block the C2 destination at the firewall and DNS level',
      'Run full endpoint detection and response (EDR) scan on the host',
      'Check for lateral movement — scan other hosts that communicated with the infected one',
      'Engage your incident response team or MSSP',
    ],
    eli5: 'A computer on your network is secretly calling home to a bad guy\'s server every few minutes, like a spy checking in with headquarters on a schedule.',
    severity: 'critical',
    tacTip: 'Document the beacon interval, destination, and payload size for your incident response report',
    fixRate: 'Isolating the host stops the beaconing, but full remediation requires malware removal',
    wiresharkFilter: 'ip.dst == <C2_IP> && frame.time_delta_displayed < 120',
    commands: [
      'netstat -an | grep <C2_IP>',
      'ss -tunap | grep <C2_IP>',
      'show ip route <C2_IP>',
    ],
  },

  // ─── PERFORMANCE: TCP WINDOW ───────────────────────────────
  tcp_zero_window: {
    code: 'TCP_ZERO_WINDOW',
    icon: '🛑',
    category: 'performance',
    what: 'TCP Zero Window detected — the receiving application\'s buffer is completely full, forcing the sender to stop transmitting.',
    why: 'Zero Window means the receiver can\'t process data fast enough. The sender must pause until the receiver advertises more buffer space. This causes application freezes, slow file transfers, and degraded user experience.',
    how: [
      'Check the receiving host\'s CPU and memory utilization — is it overloaded?',
      'Check if the application is reading from the socket fast enough',
      'Increase the TCP receive buffer: sysctl -w net.core.rmem_max=16777216',
      'Check for disk I/O bottlenecks on the receiver (slow disk = slow reads)',
      'If on a VM, check for resource contention with other VMs on the same host',
    ],
    eli5: 'The receiver\'s mailbox is completely full. The sender has to stop sending until the receiver empties some space. Everything pauses.',
    severity: 'critical',
    fixRate: 'Increasing buffer sizes or fixing application bottlenecks resolves 70% of cases',
    wiresharkFilter: 'tcp.window_size == 0',
    commands: [
      'sysctl net.core.rmem_max',
      'sysctl net.ipv4.tcp_rmem',
      'top -o %MEM',
    ],
  },

  tcp_small_window: {
    code: 'TCP_SMALL_WINDOW',
    icon: '⚠️',
    category: 'performance',
    what: 'TCP Small Window detected — the receiver is advertising a very small receive window, limiting throughput.',
    why: 'A small TCP window restricts how much data the sender can transmit before waiting for an acknowledgment. This dramatically reduces throughput, especially on high-latency links (WAN, SD-WAN tunnels).',
    how: [
      'Check the receiver\'s TCP buffer configuration',
      'Enable TCP window scaling if not already enabled',
      'Increase socket buffer sizes on the application',
      'Check for memory pressure on the receiving host',
      'For SD-WAN, verify the tunnel MTU isn\'t causing fragmentation that reduces effective window',
    ],
    eli5: 'The receiver is only accepting tiny sips of data at a time instead of big gulps. Everything slows to a crawl.',
    severity: 'warning',
    wiresharkFilter: 'tcp.window_size < 1024 && tcp.window_size > 0',
    commands: [
      'sysctl net.ipv4.tcp_window_scaling',
      'sysctl net.ipv4.tcp_rmem',
      'ss -ti',
    ],
  },

  // ─── PERFORMANCE: TCP OUT-OF-ORDER ─────────────────────────
  tcp_out_of_order: {
    code: 'TCP_OUT_OF_ORDER',
    icon: '🔀',
    category: 'performance',
    what: 'Significant TCP out-of-order packets detected on this flow — packets are arriving in the wrong sequence.',
    why: 'Out-of-order packets cause the receiver to buffer and reorder data, increasing latency. High rates trigger duplicate ACKs and unnecessary retransmissions, wasting bandwidth. Common causes: asymmetric routing, load balancer issues, or link aggregation problems.',
    how: [
      'Check for asymmetric routing — are forward and return paths different?',
      'If using ECMP or link aggregation, verify flow-based hashing is configured',
      'Check for load balancer or firewall in the path that may be splitting flows',
      'On SD-WAN, verify per-flow load balancing instead of per-packet',
      'Check interface error counters for CRC errors or link issues',
    ],
    eli5: 'Packets are arriving like shuffled cards instead of in order. The receiver has to sort them out, which slows everything down.',
    severity: 'warning',
    wiresharkFilter: 'tcp.analysis.out_of_order',
    commands: [
      'Check the forwarding path for the specific source/destination pair on the SD-WAN edge',
      'Check SD-WAN service path or routing policy for the destination subnet',
      'traceroute <destination> to verify path consistency',
    ],
  },

  // ─── INFRASTRUCTURE: VoIP ──────────────────────────────────
  voip_jitter: {
    code: 'VOIP_JITTER',
    icon: '📞',
    category: 'application',
    what: 'VoIP jitter exceeds acceptable thresholds — voice packets are arriving with inconsistent timing.',
    why: 'Jitter above 30ms causes choppy audio, robotic voices, and dropped syllables in phone calls. The jitter buffer can only compensate so much before audio quality degrades noticeably.',
    how: [
      'Check QoS configuration — voice traffic (DSCP EF/46) should be prioritized',
      'Verify the SD-WAN policy is steering voice traffic over the best path',
      'Check for bandwidth congestion on the WAN link',
      'Enable FEC (Forward Error Correction) on the SD-WAN tunnel if available',
      'Consider dedicated voice VLANs to isolate voice from data traffic',
    ],
    eli5: 'Voice packets are arriving at uneven times, like a stuttering conversation. The phone can\'t play smooth audio when packets arrive randomly.',
    severity: 'warning',
    wiresharkFilter: 'rtp',
    commands: [
      'Check QoS policy-map or traffic shaper statistics on the WAN interface',
      'Check SD-WAN per-application route statistics for voice flows',
      'Check SD-WAN tunnel SLA metrics (jitter, latency, loss)',
    ],
  },

  // ─── SECURITY: IOC ─────────────────────────────────────────
  ioc_match: {
    code: 'IOC_MATCH',
    icon: '☠️',
    category: 'security',
    what: 'An Indicator of Compromise (IOC) was matched — traffic was detected to/from a known malicious IP, domain, or hash.',
    why: 'IOC matches indicate your network is communicating with known threat infrastructure. This could mean active malware infection, data exfiltration, or an ongoing attack.',
    how: [
      'Identify the affected host and isolate it from the network immediately',
      'Verify the IOC in multiple threat intelligence sources (VirusTotal, AlienVault OTX)',
      'Check the host for malware using EDR tools',
      'Block the IOC (IP/domain) at the firewall and DNS resolver',
      'Review logs for the full timeline of communication with the IOC',
      'Check for lateral movement from the affected host',
    ],
    eli5: 'A device on your network is talking to a known bad guy. It\'s like finding your employee\'s phone number in a criminal\'s contact list.',
    severity: 'critical',
    commands: [
      'Check routing table for the IOC IP on the SD-WAN edge',
      'nslookup <IOC_domain> — verify DNS resolution',
      'netstat -an | grep <IOC_IP>  (Linux) or check active sessions on the SD-WAN edge',
    ],
  },

  // ─── PERFORMANCE: ICMP ─────────────────────────────────────
  icmp_anomaly: {
    code: 'ICMP_ANOMALY',
    icon: '📡',
    category: 'infrastructure',
    what: 'Unusual ICMP traffic patterns detected — this may indicate network reconnaissance, tunneling, or connectivity issues.',
    why: 'While ICMP is normal for ping and traceroute, unusual patterns can indicate: ICMP tunneling (data exfiltration), network scanning, or excessive unreachable messages pointing to routing problems.',
    how: [
      'Check the ICMP type and code — Type 3 (Unreachable) indicates routing issues',
      'Type 8/0 (Echo Request/Reply) in high volume may indicate scanning or tunneling',
      'Review the source IPs — are they internal or external?',
      'If excessive unreachables, check routing tables and ACLs',
      'Consider rate-limiting ICMP at the firewall if it\'s excessive',
    ],
    eli5: 'ICMP is like the postal service\'s "return to sender" system. Too many returns means something is wrong with the addresses or routes.',
    severity: 'info',
    wiresharkFilter: 'icmp',
    commands: [
      'Check ICMP statistics on the SD-WAN edge',
      'Check firewall/ACL rules for ICMP filtering',
      'ping <destination> to verify basic reachability',
    ],
  },
  // ─── VENDOR-SPECIFIC: SD-WAN ─────────────────────────────────
  advpn_shortcut_flapping: {
    code: 'ADVPN_SHORTCUT_FLAP',
    icon: '🔀',
    category: 'sdwan',
    what: 'Fortinet ADVPN (Auto Discovery VPN) shortcut tunnels are repeatedly being created and torn down between spoke sites.',
    why: 'ADVPN shortcuts are dynamic tunnels that allow spoke-to-spoke communication without going through the hub. Flapping can be caused by IKEv1 instability, aggressive DPD timers, MTU issues, or underlay path problems. Note: brief shortcut creation/teardown is NORMAL behavior — only persistent rapid flapping is a problem.',
    how: [
      'Verify IKE version is v2 (IKEv1 causes significantly more shortcut instability)',
      'Check DPD (Dead Peer Detection) settings — too aggressive causes false teardowns',
      'Verify MTU settings on the ADVPN tunnel interface',
      'Check underlay connectivity between the two spokes',
      'Review IKE negotiation logs: "diagnose debug application ike -1"',
    ],
    eli5: 'Think of ADVPN shortcuts like temporary express lanes between offices. They open when needed and close when idle. If they keep opening and closing too fast, it means the express lane construction crew has a problem.',
    severity: 'warning',
    wiresharkFilter: 'isakmp || esp',
    commands: [
      'diagnose vpn tunnel list | grep shortcut',
      'diagnose vpn ike gateway list',
      'get vpn ipsec tunnel summary',
    ],
  },
  dmpo_path_selection: {
    code: 'DMPO_PATH_SELECTION',
    icon: '🛤️',
    category: 'sdwan',
    what: 'VMware SD-WAN Dynamic Multi-Path Optimization (DMPO) is not selecting the expected path for traffic, causing suboptimal routing.',
    why: 'DMPO dynamically steers traffic across available WAN paths based on real-time quality metrics. Path selection issues can occur when Business Policy link steering conflicts with DMPO decisions, when path quality metrics are inaccurate, or when Service Class settings are misconfigured.',
    how: [
      'Check DMPO status in VCO: Monitor > Edges > [Edge] > Paths',
      'Verify Business Policy link steering settings are not conflicting with DMPO',
      'Check Service Class assignment for affected traffic (Real-Time, Transactional, Bulk)',
      'Review path quality metrics: debug.py --path_stats on the Edge',
      'Verify DMPO is enabled: Configure > Edges > [Edge] > Device > Settings > DMPO',
    ],
    eli5: 'DMPO is like a smart GPS that picks the best road based on real-time traffic. If it keeps picking a slow road, either the GPS data is wrong or someone overrode the GPS settings.',
    severity: 'warning',
    wiresharkFilter: 'ip',
  },
  gateway_handoff_failure: {
    code: 'GATEWAY_HANDOFF_FAIL',
    icon: '☁️',
    category: 'sdwan',
    what: 'VMware SD-WAN Edge is failing to establish or maintain connectivity to VeloCloud Gateways, disrupting cloud and inter-site traffic.',
    why: 'Gateway handoff failures prevent the Edge from reaching cloud resources and remote sites through the VeloCloud backbone. Common causes include NAT/firewall blocking on the underlay, Gateway capacity issues, or Edge public IP changes.',
    how: [
      'Check Gateway tunnel status in VCO: Monitor > Edges > [Edge] > Tunnels',
      'Verify Gateway health in VCO: Monitor > Gateways',
      'Check if Edge is behind a strict NAT or firewall blocking UDP 2426',
      'Verify Edge public IP has not changed (dynamic IP issue)',
      'Check Edge Events tab for Gateway-related events',
    ],
    eli5: 'Gateways are like airport hubs — your Edge needs to connect to one to reach other sites. If the connection to the hub fails, you cannot fly anywhere.',
    severity: 'critical',
    wiresharkFilter: 'udp.port == 2426',
  },
  unity_boost_failure: {
    code: 'UNITY_BOOST_FAIL',
    icon: '⚡',
    category: 'sdwan',
    what: 'Aruba EdgeConnect Unity Boost WAN optimization is not functioning, resulting in unoptimized traffic and higher bandwidth usage.',
    why: 'Unity Boost provides TCP optimization, data deduplication, and compression. Failures can be caused by expired licenses, disabled optimization settings, insufficient disk space for dedup cache, or mismatched Boost versions between endpoints.',
    how: [
      'Check Boost license status: Orchestrator > Administration > Licenses',
      'Verify optimization is enabled: Orchestrator > Configuration > Business Intent Overlays > [Overlay] > WAN Optimization',
      'Check optimization statistics: CLI "show optimization statistics"',
      'Verify both endpoints have valid Boost licenses (optimization requires both sides)',
      'Check disk space on EdgeConnect for deduplication cache',
    ],
    eli5: 'Unity Boost is like a zip compression tool for your network traffic. If it stops working, everything travels uncompressed and takes more bandwidth.',
    severity: 'warning',
    wiresharkFilter: 'tcp',
  },
  tunnel_bonding_failure: {
    code: 'TUNNEL_BONDING_FAIL',
    icon: '🔗',
    category: 'sdwan',
    what: 'Aruba EdgeConnect tunnel bonding is not functioning correctly, preventing bandwidth aggregation across multiple WAN links.',
    why: 'Tunnel bonding aggregates multiple WAN links into a single logical tunnel for increased throughput. Failures can be caused by mismatched configuration between endpoints, WAN label mismatches, or MTU issues between bonded members.',
    how: [
      'Check bonded tunnel status: Orchestrator > Monitoring > Appliances > [Appliance] > Tunnels',
      'Verify bonding is enabled on BOTH endpoints',
      'Check WAN labels match between tunnel config and WAN interface config',
      'Verify MTU settings match on all bonded members',
      'Check individual member tunnel health',
    ],
    eli5: 'Tunnel bonding is like combining multiple lanes of a highway into one super-lane. If the lanes do not line up properly, traffic cannot merge.',
    severity: 'warning',
    wiresharkFilter: 'udp',
  },
  ztp_failure: {
    code: 'ZTP_FAILURE',
    icon: '🔌',
    category: 'sdwan',
    what: 'Cisco SD-WAN Zero Touch Provisioning (ZTP) has failed — the device cannot automatically onboard to the SD-WAN fabric.',
    why: 'ZTP allows new devices to automatically discover and join the SD-WAN network. Failures are typically caused by missing DHCP options (43/44), DNS resolution failures for ztp.viptela.com, NTP clock skew causing certificate validation failures, or incorrect organization name in vManage.',
    how: [
      'Verify DHCP is providing option 43 with vBond IP address',
      'Verify DNS can resolve ztp.viptela.com from the device network',
      'Check NTP sync — clock skew >5 minutes breaks certificate validation',
      'Verify organization name matches between device and vManage',
      'If ZTP fails, use manual bootstrap via USB or CLI',
    ],
    eli5: 'ZTP is like a new employee showing up on their first day — they need their badge (certificate), the right building address (vBond), and the correct company name (organization) to get in.',
    severity: 'critical',
    wiresharkFilter: 'dns.qr == 0 && dns.qry.name contains "viptela"',
  },
  sdwan_control_down: {
    code: 'SDWAN_CONTROL_DOWN',
    icon: '🎛️',
    category: 'sdwan',
    what: 'Cisco SD-WAN control plane connections are down — the device has lost connectivity to vManage, vSmart, or vBond controllers.',
    why: 'Control plane connectivity is essential for policy distribution, route exchange (OMP), and device management. Loss of control connections means the device cannot receive policy updates, but existing data plane tunnels may continue forwarding.',
    how: [
      'Check control connections: "show sdwan control connections"',
      'Verify vBond address is correct: "show sdwan running-config | include vbond"',
      'Check certificate status: "show sdwan certificate installed"',
      'Verify system IP and organization name match vManage',
      'Check if controllers (vBond, vSmart, vManage) are reachable',
    ],
    eli5: 'The control plane is like the management office for your network. If the office phone lines go down, the workers (data tunnels) keep working but cannot get new instructions.',
    severity: 'critical',
    wiresharkFilter: 'dtls || tcp.port == 23456',
  },
  sdwan_zone_misconfiguration: {
    code: 'SDWAN_ZONE_MISCONFIG',
    icon: '🏗️',
    category: 'sdwan',
    what: 'Fortinet SD-WAN zone configuration is incorrect — interfaces are in wrong zones or firewall policies do not reference the correct zones.',
    why: 'SD-WAN zones group WAN interfaces for policy application. Misconfiguration causes traffic to be blocked by firewall policies that reference old interface names instead of zone names, or traffic to be steered through the wrong path.',
    how: [
      'Check zone membership: "config system sdwan" > "show"',
      'Verify firewall policies reference SD-WAN zones, not individual interfaces',
      'Check SD-WAN rules reference the correct zones',
      'Verify all SD-WAN members are assigned to the correct zone',
      'After zone changes, ALWAYS update firewall policies',
    ],
    eli5: 'SD-WAN zones are like departments in a building. If you move someone to the wrong department, their badge will not open the right doors (firewall policies).',
    severity: 'warning',
    wiresharkFilter: 'ip',
  },
  session_helper_conflict: {
    code: 'SESSION_HELPER_CONFLICT',
    icon: '⚙️',
    category: 'sdwan',
    what: 'FortiGate session helpers (ALGs) are interfering with application traffic traversing SD-WAN tunnels.',
    why: 'Session helpers modify packet payloads to help NAT traversal for specific protocols (SIP, H.323, TFTP). When traffic traverses SD-WAN tunnels, these modifications can break the application because the packets are already encapsulated.',
    how: [
      'Identify which session helper is active: "diagnose sys session-helper list"',
      'Check if SIP ALG is causing VoIP issues: disable with "set sip-helper disable"',
      'Disable unnecessary session helpers: "config system session-helper" > "delete <id>"',
      'Clear existing sessions after disabling: "diagnose sys session clear"',
      'Test the affected application after disabling the helper',
    ],
    eli5: 'Session helpers are like overzealous translators — they try to "help" by modifying your message, but sometimes they change it so much the recipient cannot understand it.',
    severity: 'warning',
    wiresharkFilter: 'sip || h323 || tftp',
  },
  performance_sla_violation: {
    code: 'PERF_SLA_VIOLATION',
    icon: '📊',
    category: 'sdwan',
    what: 'Fortinet SD-WAN Performance SLA thresholds have been violated — health checks are failing on one or more WAN members.',
    why: 'Performance SLA monitors define acceptable latency, jitter, and packet loss thresholds. When a member violates these thresholds, SD-WAN automatically fails over traffic to a better path. Persistent violations indicate underlay WAN quality issues.',
    how: [
      'Check health check status: "diagnose sys sdwan health-check"',
      'Identify which SLA thresholds are being violated (latency, jitter, loss)',
      'Check if the violation is on the underlay ISP link',
      'Adjust SLA thresholds if they are too tight for the link quality',
      'Verify health check probe target is reachable and responsive',
    ],
    eli5: 'Performance SLA is like a speed limit for your WAN links. When a link gets too slow or drops too many packets, SD-WAN automatically reroutes traffic to a faster road.',
    severity: 'warning',
    wiresharkFilter: 'icmp',
  },
  ssr_path_selection: {
    code: 'SSR_PATH_SELECTION',
    icon: '🧭',
    category: 'sdwan',
    what: 'Juniper Session Smart Router (SSR) is not selecting the expected path for traffic, causing suboptimal routing or connectivity issues.',
    why: 'SSR uses per-session routing based on service definitions and service-routes. Path selection issues can occur when service-routes are missing, access policies block traffic, or path-quality-filter settings are too restrictive.',
    how: [
      'Check available paths: "show service-path"',
      'Verify service definition exists for the destination',
      'Verify service-route exists on BOTH routers (routes are directional)',
      'Check access policies on the service for tenant permissions',
      'Verify BFD is enabled for path-quality-filter to work',
    ],
    eli5: 'SSR routing is like a postal service — you need both a mailbox (service) and a delivery route (service-route) for mail to arrive. Missing either one means no delivery.',
    severity: 'warning',
    wiresharkFilter: 'ip',
  },
  mist_wan_assurance_anomaly: {
    code: 'MIST_WAN_ANOMALY',
    icon: '🤖',
    category: 'sdwan',
    what: 'Juniper Mist WAN Assurance has detected an AI-identified anomaly in the WAN network behavior.',
    why: 'Mist WAN Assurance uses AI/ML to detect anomalies in network behavior that may indicate issues before they cause user impact. Anomalies may be false positives and should be validated before taking action.',
    how: [
      'Review the anomaly in Mist: WAN > Edges > [Edge] > Insights',
      'Check Marvis Actions for AI-recommended fixes',
      'Validate the anomaly against actual user-reported issues',
      'Correlate with SSR alarms and session data',
      'Apply Marvis-recommended fix if validated, or dismiss if false positive',
    ],
    eli5: 'Mist AI is like a smart security camera that flags unusual activity. Sometimes it is a real problem, sometimes the camera is just being cautious. Always check before acting.',
    severity: 'info',
    wiresharkFilter: 'ip',
  },
  prisma_access_integration: {
    code: 'PRISMA_ACCESS_INT',
    icon: '🔒',
    category: 'sdwan',
    what: 'Palo Alto Prisma SD-WAN integration with Prisma Access is failing — cloud security service connectivity is disrupted.',
    why: 'Prisma Access provides cloud-delivered security (SASE) for SD-WAN traffic. Integration failures mean traffic may bypass cloud security or be dropped, depending on path selection fallback policy.',
    how: [
      'Check Prisma Access tunnel status in Prisma SD-WAN portal',
      'Verify Prisma Access license is active',
      'Check service link configuration in Prisma SD-WAN',
      'Verify ION appliance has public IP or NAT traversal for Prisma Access tunnel',
      'Check Prisma Access portal for remote network connectivity status',
    ],
    eli5: 'Prisma Access is like a security checkpoint in the cloud. If the road to the checkpoint is blocked, traffic either takes an unsafe detour or gets stuck.',
    severity: 'critical',
    wiresharkFilter: 'esp || udp.port == 4500',
  },
  saas_app_performance: {
    code: 'SAAS_APP_PERF',
    icon: '🌐',
    category: 'application',
    what: 'SaaS application performance is degraded — users are experiencing slow response times for cloud applications like Office 365, Salesforce, or Google Workspace.',
    why: 'SaaS performance issues over SD-WAN are often caused by backhauling traffic through the data center instead of using Direct Internet Access (DIA), incorrect application classification, or suboptimal path selection.',
    how: [
      'Check application performance metrics in the SD-WAN portal',
      'Verify the SaaS app is correctly classified in application definitions',
      'Enable Direct Internet Access (DIA) for SaaS apps to avoid backhaul',
      'Set appropriate QoS priority for the SaaS application',
      'Consider Prisma Access or cloud security service for optimized SaaS paths',
    ],
    eli5: 'Backhauling SaaS traffic is like driving to the airport via the other side of town. Direct Internet Access is like taking the highway straight there.',
    severity: 'warning',
    wiresharkFilter: 'tls.handshake.extensions_server_name',
  },
  link_quality_degradation: {
    code: 'LINK_QUALITY_DEGRADE',
    icon: '📉',
    category: 'performance',
    what: 'WAN link quality has degraded — latency, jitter, or packet loss on one or more WAN links exceeds acceptable thresholds.',
    why: 'Link quality degradation is typically caused by ISP issues, link congestion, or physical layer problems. SD-WAN should automatically steer traffic away from degraded links if path selection policies are configured.',
    how: [
      'Check link quality metrics in the SD-WAN portal (latency, loss, jitter)',
      'Verify path selection policy is configured to avoid degraded links',
      'Contact ISP if metrics are consistently bad',
      'Check physical layer: cable, SFP, port errors',
      'Verify bandwidth settings match actual link capacity',
    ],
    eli5: 'Link quality degradation is like a road developing potholes. SD-WAN should automatically reroute traffic to smoother roads, but if all roads are bad, you need to call the road crew (ISP).',
    severity: 'warning',
    wiresharkFilter: 'icmp',
  },
  service_routing_issue: {
    code: 'SERVICE_ROUTING_ISSUE',
    icon: '🗺️',
    category: 'sdwan',
    what: 'Juniper SSR service routing is misconfigured — traffic cannot reach its destination because service definitions or service-routes are missing or incorrect.',
    why: 'SSR requires both a service definition (what to route) and a service-route (where to send it) for traffic to flow. Missing either component causes traffic to be dropped. Service-routes are directional, so both routers need routes for bidirectional traffic.',
    how: [
      'Verify service exists: "show config running authority service"',
      'Verify service-route exists: "show config running authority router <name> service-route"',
      'Check service-path: "show service-path" — should show available paths',
      'Verify access-policy on the service allows the tenant',
      'Ensure service-routes exist on BOTH routers for return traffic',
    ],
    eli5: 'SSR service routing is like a delivery service — you need both a shipping label (service) and a delivery route (service-route). Without both, the package sits in the warehouse.',
    severity: 'critical',
    wiresharkFilter: 'ip',
  },
  cloud_security_insertion: {
    code: 'CLOUD_SECURITY_INSERT',
    icon: '🛡️',
    category: 'sdwan',
    what: 'VMware SD-WAN Cloud Security Service (CSS) insertion is failing — traffic is not being redirected through the cloud security provider (Zscaler, Symantec, etc.).',
    why: 'CSS insertion redirects internet-bound traffic through a cloud security provider for inspection. Failures mean traffic may bypass security or be dropped, depending on Business Policy fallback settings.',
    how: [
      'Check CSS tunnel status in VCO: Monitor > Edges > [Edge] > Tunnels',
      'Verify CSS provider configuration: Configure > Network > Cloud Security Service',
      'Check if CSS provider API credentials are expired',
      'Verify Business Policy fallback action (direct vs drop)',
      'Check Edge public IP and NAT settings for CSS tunnel',
    ],
    eli5: 'Cloud Security Service is like a security checkpoint for internet traffic. If the checkpoint is closed, traffic either goes around it (unsafe) or gets stuck (dropped).',
    severity: 'critical',
    wiresharkFilter: 'gre || esp',
  },
  edge_activation_stuck: {
    code: 'EDGE_ACTIVATION_STUCK',
    icon: '🔄',
    category: 'sdwan',
    what: 'VMware SD-WAN Edge activation is stuck — the Edge appliance cannot complete onboarding to the VeloCloud Orchestrator.',
    why: 'Edge activation requires DNS resolution of the VCO hostname, HTTPS connectivity to VCO on port 443, a valid activation key, and NTP sync for certificate validation. Failures at any step prevent activation.',
    how: [
      'Verify DNS resolution of VCO hostname from Edge network',
      'Verify HTTPS connectivity to VCO on port 443',
      'Check activation key is correct and not expired',
      'Verify NTP sync on the Edge',
      'Try Edge local GUI: https://<edge-ip>/diagnostics.html for manual activation',
    ],
    eli5: 'Edge activation is like registering a new phone — you need internet access, the right account credentials, and the correct time on your clock for it to work.',
    severity: 'critical',
    wiresharkFilter: 'tls.handshake.type == 1',
  },
  ha_failover: {
    code: 'HA_FAILOVER',
    icon: '🔄',
    category: 'stability',
    what: 'SD-WAN High Availability (HA) failover has occurred or HA state is unstable — the active/standby roles are changing unexpectedly.',
    why: 'HA provides redundancy by maintaining a standby appliance that takes over if the active one fails. Unexpected failovers can be caused by HA link failures, software bugs, or resource exhaustion on the active appliance.',
    how: [
      'Check HA status on both peers',
      'Verify HA link physical connectivity',
      'Check if both peers are running the same software version',
      'Review HA event logs for failover triggers',
      'Check resource utilization (CPU/memory) on the active peer',
    ],
    eli5: 'HA is like having a backup pilot. If the backup keeps trying to take the controls, something is wrong with the handoff process.',
    severity: 'critical',
    wiresharkFilter: 'vrrp || hsrp',
  },
  bi_policy_mismatch: {
    code: 'BI_POLICY_MISMATCH',
    icon: '📋',
    category: 'sdwan',
    what: 'Aruba EdgeConnect Business Intent Overlay policy is not matching traffic as expected — flows are using the wrong overlay or bypassing SD-WAN.',
    why: 'Business Intent Overlays define how traffic should be handled. Mismatches occur when overlay priority order is wrong, match criteria are too broad/narrow, or local internet breakout settings conflict with overlay routing.',
    how: [
      'Review overlay policies: Orchestrator > Configuration > Business Intent Overlays',
      'Check which overlay each flow is using: Orchestrator > Monitoring > Flows',
      'Verify overlay priority order (higher priority matches first)',
      'Check local internet breakout settings',
      'Verify application/subnet match criteria are correct',
    ],
    eli5: 'Business Intent Overlays are like mail sorting rules. If the rules are in the wrong order, important mail might end up in the wrong bin.',
    severity: 'warning',
    wiresharkFilter: 'ip',
  },
  srx_sdwan_integration: {
    code: 'SRX_SDWAN_INT',
    icon: '🔧',
    category: 'sdwan',
    what: 'Juniper SRX firewall integration with Session Smart Router (SSR) SD-WAN is not functioning correctly.',
    why: 'SRX and SSR use different routing paradigms (zone-based vs service-based). Integration issues typically stem from security policy mismatches, routing configuration errors, or BGP peering problems between the two platforms.',
    how: [
      'Check SSR peer status: "show peers"',
      'Verify SRX security policies allow SSR traffic',
      'Check routing between SRX and SSR',
      'Verify BGP or static routes are correctly configured',
      'Check SRX zone assignments for SSR-facing interfaces',
    ],
    eli5: 'SRX and SSR speak different routing languages. Integration requires a translator (proper configuration) so they can work together.',
    severity: 'warning',
    wiresharkFilter: 'bgp || ip',
  },
};

// Map finding types to knowledge base entries
export function getKnowledgeForFinding(findingType: string): IssueKnowledge | null {
  const mapping: Record<string, string> = {
    'SYN Flood': 'ddos_syn_flood',
    'UDP Flood': 'ddos_udp_flood',
    'ICMP Flood': 'ddos_icmp_flood',
    'Horizontal': 'port_scan',
    'Vertical': 'port_scan',
    'Block': 'port_scan',
    // DHCP
    'Rogue Server': 'dhcp_rogue_server',
    'Starvation': 'dhcp_starvation',
    'NAK Storm': 'dhcp_nak_storm',
    // NTP
    'Amplification': 'ntp_amplification',
    'Stratum Change': 'ntp_stratum_change',
    'Monlist Response': 'ntp_amplification',
    // Security
    'dns_tunneling': 'dns_tunneling',
    'c2_beaconing': 'c2_beaconing',
    'ioc_match': 'ioc_match',
    // TCP Advanced
    'Zero Window': 'tcp_zero_window',
    'Small Window': 'tcp_small_window',
    'tcp_out_of_order': 'tcp_out_of_order',
    // VoIP
    'voip_jitter': 'voip_jitter',
    // ICMP
    'icmp_anomaly': 'icmp_anomaly',
  };

  const key = mapping[findingType] || findingType.toLowerCase().replace(/\s+/g, '_');
  return issueKnowledgeBase[key] || null;
}

// Get severity color classes
export function getSeverityConfig(severity: string) {
  const configs: Record<string, { bg: string; text: string; border: string; dot: string; label: string }> = {
    critical: { bg: 'bg-red-500/15', text: 'text-red-400', border: 'border-red-500/30', dot: 'bg-red-500', label: 'Critical' },
    Critical: { bg: 'bg-red-500/15', text: 'text-red-400', border: 'border-red-500/30', dot: 'bg-red-500', label: 'Critical' },
    High: { bg: 'bg-red-500/15', text: 'text-red-400', border: 'border-red-500/30', dot: 'bg-red-500', label: 'High' },
    warning: { bg: 'bg-amber-500/15', text: 'text-amber-400', border: 'border-amber-500/30', dot: 'bg-amber-500', label: 'Warning' },
    Warning: { bg: 'bg-amber-500/15', text: 'text-amber-400', border: 'border-amber-500/30', dot: 'bg-amber-500', label: 'Warning' },
    Medium: { bg: 'bg-amber-500/15', text: 'text-amber-400', border: 'border-amber-500/30', dot: 'bg-amber-500', label: 'Medium' },
    Low: { bg: 'bg-blue-500/15', text: 'text-blue-400', border: 'border-blue-500/30', dot: 'bg-blue-500', label: 'Low' },
    info: { bg: 'bg-blue-500/15', text: 'text-blue-400', border: 'border-blue-500/30', dot: 'bg-blue-500', label: 'Info' },
    Info: { bg: 'bg-blue-500/15', text: 'text-blue-400', border: 'border-blue-500/30', dot: 'bg-blue-500', label: 'Info' },
  };
  return configs[severity] || configs.info;
}

// Term definitions for tooltips
export const termDefinitions: Record<string, string> = {
  'VRRP': 'Virtual Router Redundancy Protocol — allows multiple routers to share a virtual IP address so if one router fails, another takes over automatically.',
  'HSRP': 'Hot Standby Router Protocol — Cisco\'s protocol for router redundancy, similar to VRRP.',
  'BFD': 'Bidirectional Forwarding Detection — a lightweight protocol that quickly detects link failures (much faster than routing protocol timers).',
  'STP': 'Spanning Tree Protocol — prevents network loops in Layer 2 (Ethernet) networks by blocking redundant paths.',
  'CDP': 'Cisco Discovery Protocol — Cisco\'s proprietary protocol that allows devices to share identity and capability information with neighbors.',
  'LLDP': 'Link Layer Discovery Protocol — an industry-standard protocol (like CDP) for network device discovery.',
  'QoS': 'Quality of Service — network features that prioritize certain types of traffic (like voice) over others (like file downloads).',
  'DSCP': 'Differentiated Services Code Point — a 6-bit field in the IP header used to classify and prioritize traffic.',
  'MTU': 'Maximum Transmission Unit — the largest packet size that can be sent without fragmentation. Default is 1500 bytes for Ethernet.',
  'RTT': 'Round-Trip Time — the time it takes for a packet to travel to a destination and back. Lower is better.',
  'SYN': 'The first packet in a TCP three-way handshake. The client says "I want to connect."',
  'ACK': 'Acknowledgment — confirms receipt of data. Part of TCP\'s reliability mechanism.',
  'TLS': 'Transport Layer Security — encryption protocol that secures data in transit. Successor to SSL.',
  'IPsec': 'Internet Protocol Security — encrypts and authenticates IP packets. Used by most SD-WAN tunnels.',
  'GRE': 'Generic Routing Encapsulation — a tunneling protocol that encapsulates packets inside other packets.',
  'OMP': 'Overlay Management Protocol — Cisco SD-WAN\'s routing protocol for the overlay network.',
  'TLOC': 'Transport Locator — a Cisco SD-WAN concept representing a WAN transport connection (MPLS, Internet, LTE).',
  'vSmart': 'Cisco SD-WAN controller that distributes routing and policy information to edge devices.',
  'vBond': 'Cisco SD-WAN orchestrator that handles initial device authentication and discovery.',
  'FEC': 'Forward Error Correction — adds redundant data to detect and correct errors without retransmission.',
  'uRPF': 'Unicast Reverse Path Forwarding — a security feature that drops packets with spoofed source IP addresses.',
  'DNS': 'Domain Name System — translates domain names (like google.com) to IP addresses.',
  'ARP': 'Address Resolution Protocol — maps IP addresses to MAC addresses on a local network.',
  'NAT': 'Network Address Translation — converts private IP addresses to public ones for internet access.',
  'MPLS': 'Multiprotocol Label Switching — a routing technique that uses labels instead of IP addresses for fast forwarding.',
  'BGP': 'Border Gateway Protocol — the routing protocol that connects different networks (autonomous systems) on the internet.',
  'OSPF': 'Open Shortest Path First — a link-state routing protocol used within a single organization\'s network.',
  'RTP': 'Real-time Transport Protocol — carries voice and video data over IP networks.',
  'SIP': 'Session Initiation Protocol — sets up, manages, and tears down voice/video calls over IP.',
  'MOS': 'Mean Opinion Score — a measure of voice call quality on a scale of 1 (bad) to 5 (excellent).',
};
